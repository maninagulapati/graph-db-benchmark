"""Adapter package.

Adapters are resolved dynamically by src/benchmark/runner.py from the
`adapter: "module:ClassName"` strings in config/databases.yaml, so nothing
needs to be imported or registered here manually.
"""
